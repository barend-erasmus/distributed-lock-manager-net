<?xml version="1.0"?>
<package xmlns="http://schemas.microsoft.com/packaging/2013/05/nuspec.xsd">
    <metadata>
        <!-- The identifier that must be unique within the hosting gallery -->
        <id>DistributedLockManager.NET</id>

        <!-- The package version number that is used when resolving dependencies -->
        <version>1.0.1</version>

        <!-- Authors contain text that appears directly on the gallery -->
        <authors>Barend Erasmus</authors>

        <!-- Owners are typically nuget.org identities that allow gallery
                users to easily find other packages by the same owners.  -->
        <owners>Barend Erasmus</owners>

        <!-- License and project URLs provide links for the gallery -->
        <licenseUrl>https://opensource.org/licenses/MIT</licenseUrl>
        <projectUrl>https://github.com/barend-erasmus/distributed-lock-manager-net/projectUrl>

        <!-- The icon is used in Visual Studio's package manager UI -->
        <iconUrl>https://github.com/barend-erasmus/distributed-lock-manager-net/raw/master/images/icon.jpg</iconUrl>

        <!-- If true, this value prompts the user to accept the license when
                installing the package. -->
        <requireLicenseAcceptance>false</requireLicenseAcceptance>

        <!-- Any details about this particular release -->
        <releaseNotes>Initial Release</releaseNotes>

        <!-- The description can be used in package manager UI. Note that the
                nuget.org gallery uses information you add in the portal. -->
        <description>.NET Client for Distributed Lock Manager Server</description>

        <!-- Copyright information -->
        <copyright>Copyright © 2018 Developer's Workspace PTY LTD</copyright>

        <!-- Tags appear in the gallery and can be used for tag searches -->
        <tags>lock locks mutex mutexes semaphore semaphores manager thread safe</tags>

        <!-- Dependencies are automatically installed when the package is installed -->
        <dependencies>
        </dependencies>
    </metadata>

    <!-- A readme.txt to display when the package is installed -->
    <files>
        
    </files>
</package>